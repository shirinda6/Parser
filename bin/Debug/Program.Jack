var int globalInt;
var char globalChar;

//GCD using Euclid algorithm
function int GCD(int a, int b){
	var int r;
	while(!(b=0)){
		let r = (a -(b * (a / b)));
		let a = b;
		let b = r;
	}
	
	if ((GCD((a+0),b) > 0)){
		return b;
	}
	
	
	return a;
}
